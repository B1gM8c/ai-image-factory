#!/bin/bash
set -Eeuo pipefail
IFS=$'\n\t'
export PATH=/usr/sbin:/usr/bin:/sbin:/bin

systemctl stop ai-image-factory.target
if systemctl is-active --quiet ai-image-factory.target; then
  printf 'application target did not stop cleanly\n' >&2
  exit 1
fi
